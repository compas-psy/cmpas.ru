-- Состояние подключения календаря: когда провайдер отказал и как.
--
-- Дефект Ф10 книги «Витрина и машинное отделение». Google отзывает доступ по
-- многим поводам — сменили пароль, полгода не заходили, отозвали разрешение;
-- Яндекс перестаёт пускать при смене пароля приложения. В этот момент
-- выгрузка встреч прекращается, отказ пишется в журнал без персональных
-- данных — и дальше журнала не идёт. Подключение остаётся помеченным
-- рабочим, на экране горит «Подключён», специалист продолжает верить, что
-- его встречи попадают в календарь.
--
-- Код отказа — только из фиксированного набора, текст провайдера сюда не
-- пишется: в теле ответа Google или CalDAV лежит само событие с именем
-- клиента.
ALTER TABLE "CalendarIntegration" ADD COLUMN "lastErrorAt" TIMESTAMP(3);
ALTER TABLE "CalendarIntegration" ADD COLUMN "lastErrorCode" TEXT;
