-- Основной мессенджер клиента: "telegram" | "max".
--
-- До этой правки один и тот же человек мог получить одно событие двумя
-- сообщениями: пути отправки писали И в Telegram, И в MAX. Теперь канал
-- один, и он назван — тот, через который клиент пришёл последним.
--
-- Пусто у всех, кто привязался раньше: для них работает прежний порядок
-- (Telegram, иначе MAX), и трогать их записи ради нового поля незачем.
ALTER TABLE "DiaryClient" ADD COLUMN IF NOT EXISTS "preferredChannel" TEXT;
