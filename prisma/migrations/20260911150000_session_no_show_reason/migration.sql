-- Причина неявки словами специалиста.
--
-- Кнопка «Причина» на карточке существовала и открывала общую форму записи,
-- где причину было негде ввести: поля не было. Добавление, ничего не
-- переписывающее: у всех существующих записей остаётся NULL.
ALTER TABLE "DiarySession" ADD COLUMN "noShowReason" TEXT;
