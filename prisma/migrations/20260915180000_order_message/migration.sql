-- Сообщение, которое человек пишет в форме заказа ежедневника.
--
-- Дефект Ф3 книги «Витрина и машинное отделение». Поле на экране было, и
-- подсказка прямо звала написать главное — количество экземпляров и вопрос
-- по доставке. Форма это хранила, а дальше текст исчезал: приём заказа
-- такого поля не принимал, колонки не было, в уведомление он не попадал.
-- Отвечающий видел имя, телефон и способ связи и перезванивал с вопросом,
-- который человек уже задал.
--
-- Колонка отдельная от notes: notes — пометки того, кто обрабатывает заказ,
-- и складывать в одно поле слова человека и слова оператора значит однажды
-- потерять и то и другое. У существующих строк остаётся NULL.
ALTER TABLE "Order" ADD COLUMN "message" TEXT;
