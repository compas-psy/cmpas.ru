-- Задача 24: серверное состояние онбординга практики.
--
-- Две отметки времени, которых не видно в данных практики: когда ссылкой
-- действительно поделились и когда человек закрыл подсказку. Остальные три
-- шага чек-листа вычисляются из клиентов, расписания и записей и колонок не
-- требуют.
--
-- Существующие практики получают NULL: «ещё не делились» и «не скрывали» —
-- это правда для всех, кто был до появления полей.
ALTER TABLE "PsychologistSettings" ADD COLUMN "bookingLinkSharedAt" TIMESTAMP(3);
ALTER TABLE "PsychologistSettings" ADD COLUMN "onboardingDismissedAt" TIMESTAMP(3);
