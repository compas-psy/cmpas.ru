-- Уведомление помнит, о каком событии оно.
--
-- Гашение повторов ключевалось по заголовку и подзаголовку. Подзаголовок —
-- это имя клиента, название документа и версия, поэтому второе вручение ТОГО
-- ЖЕ документа тому же клиенту давало тот же ключ, и настоящая подпись под
-- ним считалась повтором уже показанного. 08.09.2026 так пропала подпись под
-- информированным согласием: отметка в карточке проставилась, уведомления не
-- было.
--
-- Только добавление колонки и индекса. Существующие строки получают NULL:
-- новая логика смотрит только на refId, поэтому старые записи ничего не
-- гасят.
ALTER TABLE "PracticeNotification" ADD COLUMN IF NOT EXISTS "refId" TEXT;

CREATE INDEX IF NOT EXISTS "PracticeNotification_psychologistId_type_refId_idx"
  ON "PracticeNotification" ("psychologistId", "type", "refId");
