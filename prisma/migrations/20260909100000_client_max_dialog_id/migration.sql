-- Идентификатор диалога MAX у клиента.
--
-- Нужен, чтобы спросить у MAX аватарку: GET /chats/{chat_id} отдаёт
-- dialog_with_user только по идентификатору диалога, а maxChatId — это
-- user_id. Диалог приходит в каждом входящем сообщении и заполняется сам.
--
-- Только добавление колонки: существующие строки получают NULL, и до
-- первого сообщения клиента у него просто остаются инициалы.
ALTER TABLE "DiaryClient" ADD COLUMN IF NOT EXISTS "maxDialogId" TEXT;
