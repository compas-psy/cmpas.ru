-- Когда исход встречи назвал ЧЕЛОВЕК, а не догадалась система.
--
-- status='completed' означал две разные вещи: специалист нажал «Была» — или
-- сервер сам перевёл сессию через 15 минут после её конца
-- (settlePastSessionsForPsychologist). Различить было нечем, и карточка не
-- могла показать развилку: она не знала, отвечен ли вопрос «состоялась ли
-- встреча» или ещё только предстоит.
--
-- Пусто — система предположила. Заполнено — человек сказал.
--
-- Только добавление, без переноса данных: у прошлых сессий отметки человека
-- не было, и выдумывать её задним числом нельзя.
ALTER TABLE "DiarySession" ADD COLUMN IF NOT EXISTS "outcomeRecordedAt" TIMESTAMP(3);
