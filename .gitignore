# dependencies
/node_modules
/.pnp
.pnp.js

# testing
/coverage

# next.js
/.next/
/out/

# production
/build

# misc
.DS_Store
*.pem

# debug
npm-debug.log*
yarn-debug.log*
ts-debug.log*

# local env files
.env*.local
.env

# vercel
.vercel

# typescript
*.tsbuildinfo
next-env.d.ts

# vps keys
gh_deploy_key
gh_deploy_key.pub
antigravity_key
antigravity_key.pub
id_rsa
id_rsa.pub
id_ed25519
id_ed25519.pub

/src/generated/prisma

# Geist font: temp files only; the .ttf binaries are committed under res/font/
/android/app/src/main/res/font/*.tmp

# Materialized from deploy/singbox-config.template.json at deploy time —
# contains real mieru VPN credentials, never commit the real file.
/deploy/singbox-config.json

# Граф кода graphify — генерируемый артефакт, в репозиторий не коммитится
graphify-out/

# Разовые скрипты локальной проверки вёрстки (снимки экранов, замеры контраста).
# Лежат в корне, потому что оттуда резолвится playwright; в репозитории им
# места нет — это черновики одного прогона, а не часть проекта.
/*-review.mjs
/live-shot.mjs
/shot.mjs
/measure.mjs
/diag.mjs
/audit-*.mjs
/z*.mjs
