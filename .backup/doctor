Файл-триггер диагностики. Изменение в main снимает состояние базы
и кладёт отчёт в docs/ops/db-doctor.md. Ничего не меняет, только читает.
Прогон 2: вывод снимается напрямую по ssh, а не через outputs действия.
