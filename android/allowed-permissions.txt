# Разрешения приложения ПРАКТИКА (ru.cmpas.app).
#
# Этот файл — не описание намерений, а список, по которому сторож
# (scripts/check-apk.sh) проверяет ГОТОВЫЙ пакет. Разрешение, попавшее в APK и
# не названное здесь, роняет сборку — в том числе пришедшее из библиотеки при
# слиянии манифестов, которого никто не читал глазами.
#
# Правило одно: остаётся только то, без чего приложение не работает, и у
# каждого — одна строка обоснования с местом в коде.
#
# Формат: одно разрешение в строке, комментарии после #.

# Весь обмен с сервером: Retrofit/OkHttp, авторизация, синхронизация клиентов и
# сессий, очередь досылки. Без него не откроется ни одно соединение.
# di/NetworkModule.kt, data/api/CompasApi.kt
android.permission.INTERNET

# Голосовые заметки к сессии. Путь достижим из интерфейса, а не задел на
# будущее: режим «Голосом» — один из трёх в NoteMode, ничем не скрыт и не
# заблокирован, кнопка ведёт в реальный MediaRecorder с источником MIC.
# presentation/notes/PostSessionNoteScreen.kt: enum NoteMode (44),
# VoiceCapture (303-312), toggleVoiceRecording (168), startVoiceRecording (79-106)
#
# ЦЕНА, ПРИНЯТАЯ СОЗНАТЕЛЬНО: это единственное разрешение, из-за которого при
# установке запускается проверка Play Protect — человек видит предупреждение
# вместо тихой установки. Проверка проходит. Уберём это разрешение — установка
# станет молчаливой, но исчезнут голосовые заметки.
android.permission.RECORD_AUDIO

# УБРАНО 23.08.2026: android.permission.POST_NOTIFICATIONS
# Приложение не показывает ни одного системного уведомления: ни
# NotificationManager, ни NotificationCompat, ни NotificationChannel в коде нет,
# сервисов и ресиверов в манифесте нет. «Центр уведомлений» на экране — это
# список PracticeNotification из API, то есть обычный элемент интерфейса.
# Методы registerFcmToken/unregisterFcmToken в CompasApi объявлены, но не
# вызываются нигде, а Firebase Messaging к сборке не подключён.
# Разрешение, о котором спрашивают человека и которым не пользуются, — плохой
# обмен: он платит вниманием, не получая ничего.
