/**
 * Реквизиты оператора — в одном месте, потому что расходились.
 *
 * До этого они были переписаны в каждом документе отдельно, и разошлись
 * ровно так, как расходится всё продублированное:
 *
 *   • в согласии на рекламу стояли ПЛЕЙСХОЛДЕРЫ «{{ОГРНИП}}» и «{{ИНН}}» —
 *     то есть документ, который человек подписывает, называл оператора
 *     двумя фигурными скобками;
 *   • в оферте и политике в качестве адреса оператора стояла ЛИЧНАЯ почта
 *     учредителя на gmail — по четырём местам;
 *   • сервис назывался «CMPAS» латиницей, хотя система называется СИМПАС.
 *
 * Документ, в котором не назван оператор, не выполняет своей работы: по
 * 152-ФЗ субъект должен знать, кому он даёт согласие и куда обращаться за
 * отзывом. Поэтому это не косметика.
 *
 * Менять значения здесь и только здесь. Что ни один документ не разойдётся с
 * этим файлом снова, стережёт tests/legal-requisites.test.ts.
 */

/** Как система называется в текстах документов. Не «CMPAS» и не «КОМПАС». */
export const SERVICE_NAME = 'СИМПАС';

/** Оператор персональных данных. */
export const OPERATOR_NAME = 'ИП Мартынов И.Н.';
export const OPERATOR_OGRNIP = '324774600361792';
export const OPERATOR_INN = '505003226577';

/**
 * Адрес для юридически значимых обращений: отзыв согласия, запрос по 152-ФЗ,
 * претензия по оферте.
 *
 * Отдельный адрес, а не личная почта: он переживает смену человека, читающего
 * ящик, и не раскрывает частный контакт всем, кто открыл политику.
 */
export const OPERATOR_EMAIL = 'legal@cmpas.ru';

export const PUBLIC_SITE = 'cmpas.ru';
