/**
 * ЦЕНА ПОДПИСКИ — В ОДНОМ МЕСТЕ.
 *
 * До этого модуля цена была вписана в пяти: витрина биллинга (дважды),
 * лендинг, структурированные данные для поисковиков, справка — и, отдельно
 * от всех, сумма списания в `src/lib/tinkoff.ts`. Совпадали они только
 * потому, что их меняли одним заходом.
 *
 * Цена расходится не тогда, когда её забывают поменять везде, а тогда,
 * когда расхождение НЕЗАМЕТНО. Здесь оно было бы незаметно до самого
 * страшного места: кнопка обещает одну сумму, банк списывает другую.
 * Человек видит это уже на странице оплаты, и доверие теряется мгновенно.
 *
 * Поэтому цена ровно одна, а витрина и списание считаются из неё.
 * Сторож — tests/billing-price.test.ts: он падает, если сумма вписана
 * руками где-то ещё.
 */

/** Цена тарифа «Практика» в рублях за месяц. */
export const PRACTICE_PRICE_RUB = 1490;

/** Она же в копейках — в этом виде её принимает эквайринг. */
export const PRACTICE_PRICE_KOPECKS = PRACTICE_PRICE_RUB * 100;

/** «1 490 ₽» — с неразрывным пробелом, чтобы число не отрывалось от знака. */
export const PRACTICE_PRICE_LABEL = `${PRACTICE_PRICE_RUB.toLocaleString('ru-RU')} ₽`;
