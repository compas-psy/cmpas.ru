Client onboarding polish status

Implemented:
- keep new client in focus during first booking flow
- format Telegram messages with hidden links and disabled previews
- use clean plain text for manual and MAX sending
- record document acceptance when the client opens the document link
- improve the mobile document page

Follow-up:
- if needed, replace the temporary server-side selection marker with a client-side ref in clients/page.tsx.